export interface Deal {
  property: object,
  agent: object,
  client: object,
  rent: string,
  rate: string,
  commission: string,
  flat_fee: string,
  move_date: string,
  unit_no: string,
  lease_term: string,
}